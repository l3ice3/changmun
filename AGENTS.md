# AGENTS.md — Codex 작업 지침 (창문 changmun)

창업 지원금 큐레이션 서비스 모노레포. Codex가 이 레포에서 **구현하거나 PR을 리뷰할 때** 따르는 지침이다.

---

## 1. 구현 작업 시 (코드 작성)

1. **프로젝트 가드레일은 `CLAUDE.md`를 따른다** — 파일명과 무관하게 모든 AI·사람 공통 규칙이다.
   제1원칙(스펙이 모호하면 멈추고 질문, 추측 구현 금지) · 절대 규칙 10개 · 문서 맵 · 작업 흐름(FR 단위, AC 자가 판정).
2. **Java(apps/api) 코딩 규칙은 구현 전에 반드시 참조한다**:
   - 상시 규칙(원본): `docs/rules/rules-core.md`
   - 상세·이유·예시: `docs/rules/rules-full.md` — 작업 종류별 해당 섹션만 발췌 참조
   - 테스트 작성: `docs/rules/testing.md` · 저장/트랜잭션/멱등: `docs/rules/persistence.md`
   - 커밋 전 `cd apps/api && ./gradlew check` (checkstyle+pmd+test) 통과 필수 — 실패 시 PR 머지 불가
3. **앱별 상세 규칙**: `.claude/rules/ingest.md` · `.claude/rules/api.md` · `.claude/rules/web.md` (해당 앱 작업 시 필독)
   - 협업 흐름: `docs/rules/git.md` (브랜치·커밋·PR·머지·이슈·라벨·AI 표기·Flyway 타임스탬프) · 식별자 표준어: `docs/rules/glossary-dev.md`
   - 로컬 강제 hook: `.claude/hooks/README.md` (보호 브랜치 commit/push 차단 · 절대규칙 위반 피드백)
4. 계약 문서가 단일 진실이다: 스키마=`docs/data-model.md`(LOCKED), API=`docs/api-spec.md`, 요구사항=`docs/PRD.md`, 완료 판정=`docs/AC.md`.

---

## 2. 코드 리뷰 시 (PR 리뷰)

> 원칙: **셀 수 있는 규칙은 봇(정적 분석), 의미·설계 판단이 필요한 규칙은 LLM 제안과 사람.**
> 봇에 의미 판단을 시키면 오탐이 쏟아져 신뢰를 잃는다. 신뢰를 잃은 봇은 무시당한다.

### A그룹 — 정적 분석이 자동 차단 (너는 지적하지 마라)
기계적으로 셀 수 있어 CI(static-analysis.yml: Checkstyle/PMD/ArchUnit)가 **하드 게이트**로 이미 차단한다.
**중복 지적 금지** — 네가 다시 말하면 노이즈다.

- indent depth > 2
- 메서드 라인 수 > 20
- 메서드 인자 수 > 4
- 인스턴스 변수 > 5
- `else` / `switch`-`case` / 삼항 연산자 사용
- 축약·약어, 한 글자 변수명
- 네이밍 컨벤션(케이스, 길이)
- 배열 사용(컬렉션 권장 위치)
- 디미터 위반 중 **체이닝 형태**(`a.getB().getC()`)만 — 패턴으로 탐지 가능
- 계층 의존 위반(도메인→repository, controller→repository 등 — ArchUnit)

### B그룹 — 네가 집중할 것 (의미·맥락 판단, 기계가 못 잡음)
사람 리뷰의 핵심 영역이기도 하다. 너는 **제안 코멘트**로 사람의 판단을 돕는다.

- **SRP** — "변경 이유가 다르면 분리" (변경 이유가 다른 코드가 한 클래스에 섞였는가)
- **Tell, Don't Ask** — 데이터를 꺼내 외부에서 판단했는가 (의미 분석 필요, 체이닝 탐지와 별개)
- **다형성 전환 시점** — 같은 타입 분기가 2곳 이상 반복되는가 (의도 파악 필요)
- **추상화 레벨 결정** — 공통 기능을 상위에서 구현하는 게 적절한가
- **불변 가능성** — 변경 여지 없는 상태가 가변으로 남았는가
- **트랜잭션 묶음 경계** — 정합성을 타이트/느슨 중 무엇으로 볼지
- **테스트의 행위 완결성** — 테스트가 행위를 검증하는가, 단순 위임 Controller를 불필요하게 테스트하는가 (커버리지 수치로는 못 잡음)

### C그룹 — 경고 수준으로만 (확신 낮은 영역)
- 성급한 추상화 (분기 1곳인데 인터페이스 도입)
- 원시값 포장 누락 중 "도메인 의미가 있어 보이는" 값
- Null Object로 바꿀 만한 null 반환

### 리뷰 수행 규칙
1. 각 지적은 **파일/라인을 명시**하고, 근거가 되는 규칙 문서 섹션(`docs/rules/rules-full.md` 등)을 인용하라.
2. **근거가 약하면 지적하지 말고 넘어가라.** 확신 있는 것만 말하라 — 근거 약한 차단·지적은 무시와 우회를 부른다.
3. 프로젝트 특화 체크: `CLAUDE.md` 절대 규칙 위반(status 저장, raw 가공, is_canonical 누락, 필드명 임의 변경,
   "받을 수 있어요"류 카피, Out-of-Scope 기능 추가)이 diff에 보이면 **이것은 우선 지적 대상**이다.
4. **너는 제안만 한다 — 절대 머지를 막지 마라.** 리뷰 마지막에 "이 리뷰는 제안이며 머지를 막지 않는다"를 명시하라.

### 운영 원칙 (팀 참고)
1. **하드 게이트(A)와 소프트 제안(B/C)을 분리**한다. 섞으면 둘 다 무뎌진다.
2. 봇 지적의 **오탐률을 측정**한다. 특정 규칙의 오탐이 높으면 사람 전용으로 내리거나 경고로 완화한다.
3. **위반 로그를 쌓는다.** 자주 어기는 규칙 = `rules-core.md` 상시 주입 후보, 한 번도 안 어기는 규칙 = 상시본에서 제외 후보.
