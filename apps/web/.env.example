# 서빙 API 베이스 URL (브라우저·서버 공통). 기본값은 로컬 Spring(apps/api).
NEXT_PUBLIC_API_BASE=http://localhost:8080/api/v1

# 사이트 공개 URL — 메타태그 metadataBase·sitemap·robots에 사용. 배포 시 실제 도메인으로 교체.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
